projection;
strict ( 2 );
use draft;

define behavior for ZXE2_C_FILES alias Files
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}